// "enum" representing directions used in searching for neighboring nodes in quadtree
export const Dir = Object.freeze({
    UP: Symbol("up"),
    DOWN: Symbol("down"),
    LEFT: Symbol("left"),
    RIGHT: Symbol("right")
})